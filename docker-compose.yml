version: "3"

services:
  web:
    build: .
    container_name: djangoplicity-events
    command: ["./scripts/command-dev.sh"]
    volumes:
      - ./djangoplicity:/app/djangoplicity
      - ./test_project/:/app/test_project
      - ./htmlcov:/app/htmlcov
      - ./.coveragerc:/app/.coveragerc
      - ./tests:/app/tests
    ports:
      - "8001:8000"
    depends_on:
      - db
  
  db:
    image: postgres:10.5
    container_name: djangoplicity-events-db
    restart: always
    environment:
      POSTGRES_DB: djangoplicity-events
      POSTGRES_USER: djangoplicity-events
      POSTGRES_PASSWORD: djangoplicity-events
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "51432:5432"

volumes:
  pgdata:
